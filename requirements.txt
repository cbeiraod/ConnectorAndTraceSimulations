# Optimization and Math
optuna>=3.0.0
numpy>=1.20.0
scipy>=1.7.0

# Plotting and Data handling
matplotlib>=3.5.0
h5py>=3.0.0
pandas>=1.3.0

# python shenanigans
Cython<3.0
numpy
setuptools
wheel

# NOTE: openEMS and CSXCAD python modules are NOT in this list.
# They must be provided by the system (via Docker, apt, or Homebrew)